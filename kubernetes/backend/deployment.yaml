apiVersion: apps/v1
kind: Deployment
metadata:
  name: book-app-backend-deployment
  labels:
    app: book-app-backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: book-app-backend
  template:
    metadata:
      labels:
        app: book-app-backend
    spec:
      containers:
      - name: book-app-backend
        image: tapashranjannandi/book-app-backend:latest
        env: 
        - name: PORT
          value: "4001"
        - name: MongoDBURI
          value: "mongodb+srv://tapashbariflo_db_user:yuYD21YBP1bziUSx@cluster1.ynrsg2p.mongodb.net/bookStore?retryWrites=true&w=majority&appName=Cluster1"
        ports:
        - containerPort: 4001
