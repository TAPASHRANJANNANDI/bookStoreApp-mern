apiVersion: v1
kind: Service
metadata:
  name: book-app-frontend-service
spec:
  selector:
    app: book-app-frontend
  ports:
    - protocol: TCP
      port: 5173
      targetPort: 5173
  type: LoadBalancer
  
